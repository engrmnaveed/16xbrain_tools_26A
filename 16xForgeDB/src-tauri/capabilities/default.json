{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs + native save dialog",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
