# OS
.DS_Store
Thumbs.db

# Editors / IDE
.idea/
.vscode/
*.swp

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Build output
dist/
build/
release/
*.tsbuildinfo

# electron-builder reads app icons (icon.icns/.ico/.png) from each app's
# build/ dir (its "buildResources" folder) — that's committed source, not
# build output, so un-ignore it specifically.
!16x-RAG-Explorer/build/
!16x-Swarm/build/
!16xDataFlux/build/
!16xSelfHeal/build/
!16xShemaMind/build/

# Rust / Tauri
src-tauri/target/
src-tauri/gen/schemas/

# Env / secrets
.env
.env.*
!.env.example

# Misc
*.db
*.db-wal
*.db-shm
*.tmp
